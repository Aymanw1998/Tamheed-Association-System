# Tamheed client env example
# Copy this file to ".env" in this same folder (client/.env) and fill in real values.
# Run "npm run setup" from the repo root to do this automatically.

REACT_APP_SERVER_URI=http://localhost:5000
